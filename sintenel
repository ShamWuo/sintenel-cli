#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

if [ ! -f "$DIR/dist/sintenel.cjs" ]; then
    echo "◈ [SYSTEM] Sintenel bundle not found. Building..."
    npm run bundle
fi

node "$DIR/dist/sintenel.cjs" "$@"
